# Plan 18 — Update org/marketplace identifiers (bitkentech rebrand)

## Context

The GitHub repo for this project is moving from `pramodbiligiri/devostat` to `bitkentech/devostat`, and the sibling marketplace repo is moving from `pramodbiligiri/claude-plugins` to `bitkentech/claude-plugins`. Additionally, the marketplace *name* (used in `/plugin install devostat@<marketplace>`) is being changed from `pramodb-plugins` to `bitkentech` so that the install identifier matches the owning org — making it easier for users to recognize the same identity across places.

This plan updates every repo-internal reference that must reflect the new identifiers. The sibling marketplace repo (`bitkentech/claude-plugins`) itself is out of scope — its `marketplace.json` will be updated separately when that repo is created/renamed.

Following the plan-15 pattern, URLs are being updated in anticipation of the rename (tracked as a separate action for the human to execute at the GitHub level). Historical plan files in `.agents/plans/` are left unchanged — they reflect identifiers at their time of writing. Session transcripts under `docs/session/` are likewise immutable.

## Scope

### In scope — three identifier changes
1. **Repo URL:** `pramodbiligiri/devostat` → `bitkentech/devostat`
2. **Marketplace repo:** `pramodbiligiri/claude-plugins` → `bitkentech/claude-plugins`
3. **Marketplace name:** `pramodb-plugins` → `bitkentech` (keeping the install command `devostat@bitkentech` aligned with the owning org)

### Out of scope
- Maven `groupId` values (`com.github.pramodbiligiri` in the four `pom.xml` files). Artifact coordinate — user explicitly declined changing this.
- `.agents/plans/plan-*.md` — immutable audit trail.
- `docs/session/*.jsonl` — immutable session transcripts.
- The marketplace.json inside the sibling `bitkentech/claude-plugins` repo — handled there, not here.
- `build/` output — regenerated by Maven from `plugin-resources/src/main/resources/`; do not hand-edit.
- Local cache path references in historical plans (e.g. `~/.claude/plugins/cache/pramodb-plugins/...`) — historical.

## Files to modify

### Repo URL change (`pramodbiligiri/devostat` → `bitkentech/devostat`)

| File | Lines | Field |
|---|---|---|
| `.claude-plugin/plugin.json` | 7, 8 | `homepage`, `repository` |
| `plugin-resources/src/main/resources/claude-plugin/plugin.json` | 7, 8 | `homepage`, `repository` (Maven source — regenerates `build/.claude-plugin/plugin.json`) |
| `plugin-resources/src/main/resources/claude-plugin/marketplace.json` | 6, 15 | `owner.url`, `author.url` |

### Marketplace repo change (`pramodbiligiri/claude-plugins` → `bitkentech/claude-plugins`)

| File | Line | Context |
|---|---|---|
| `README.md` | 22 | `/plugin marketplace add pramodbiligiri/claude-plugins` |

### Marketplace name change (`pramodb-plugins` → `bitkentech`)

| File | Lines | Context |
|---|---|---|
| `README.md` | 27 | `/plugin install devostat@pramodb-plugins` |
| `DEVELOPMENT.md` | 43, 60 | `"devostat@pramodb-plugins": false/true` JSON snippets |

## Notes on the two `plugin.json` files

- `.claude-plugin/plugin.json` at the repo root is a standalone committed file (not generated).
- `plugin-resources/src/main/resources/claude-plugin/plugin.json` is filtered by `plugin-resources/pom.xml` (execution `copy-plugin-meta`) into `build/.claude-plugin/plugin.json`. The `build/` copy is gitignored.
- Both must be updated to stay in sync; `build/` regenerates automatically on `mvn process-resources`.

## Risk calibration (default suggestions)

All three tasks are **Low risk** — string substitutions in config/docs with no logic change. Suggested ordering:

1. **Task 1 — Repo URL updates** (Low). Three files, pure string substitution. Pairs naturally because all three touch the same identifier.
2. **Task 2 — Marketplace repo + name in README** (Low). One file (`README.md`), two distinct substitutions on lines 22 and 27.
3. **Task 3 — Marketplace name in DEVELOPMENT.md** (Low). One file, two occurrences on lines 43 and 60.

These can reasonably be bundled into a single commit per the user's earlier feedback preference for avoiding churn on trivially-related changes, but keeping them as three separately committable tasks preserves clean rollback points.

Awaiting human override of risk levels before proceeding to Phase 2.

## Verification

1. **grep clean-up check** — after edits, `pramodbiligiri/devostat` and `pramodb-plugins` should only appear in:
   - `.agents/plans/plan-*.md` (historical)
   - `docs/session/*.jsonl` (historical)
   - `pom.xml` groupIds (out of scope)
   - `plan-18-tasks.xml` itself (when tracking task text)
   Any other appearance indicates a missed edit.
2. **JSON validity** — `python3 -m json.tool` (or `jq .`) on each modified `.json` file must succeed.
3. **Maven build** — `mvn process-resources` in the repo root must succeed and `build/.claude-plugin/plugin.json` must contain the new URL. Confirms the filtered copy regenerated cleanly.
4. **Visual README render** — open `README.md` locally; Installation section should read coherently with the new `bitkentech/claude-plugins` and `devostat@bitkentech` identifiers.

## Task tracking mode

**Hybrid per user direction:**
- **Top-level feature** → new Linear issue in the `devostat — Backlog & Roadmap` project (team `PB` / `Pb-default`). Serves as the permanent backlog item this plan satisfies (Core Invariant #3).
- **Per-task execution tracking** → local `.agents/plans/plan-18-tasks.xml` (same workflow as plan-17).

No `[agent]` Linear project is being created — per user direction, only the top-level backlog issue goes to Linear; agent-level tasks stay in the XML file.

### Linear feature issue

- **Issue:** [PB-266](https://linear.app/pb-default/issue/PB-266/update-repomarketplace-identifiers-for-bitkentech-rebrand)
- **Project:** `devostat — Backlog & Roadmap`
- **Team:** `Pb-default` (`PB`)
- **Branch:** `t/pb-266-bitkentech-rebrand`
